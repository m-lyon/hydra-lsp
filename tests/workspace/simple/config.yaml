# @hydra
# Basic Hydra configuration for testing

model:
  _target_: torch.nn.Linear
  in_features: 10
  out_features: 5

optimizer:
  _target_: torch.optim.Adam
  lr: 0.001
  betas: [0.9, 0.999]

dataset:
  _target_: my_module.DataLoader
  batch_size: 32
  shuffle: true
